{
  "id": "T-HQU1",
  "title": "HQ-UNION build 1 rider + HO-1 bench placement, two players: eight of the host's bench workers are placed on the client's shop in ONE scan - the pacing budget defers the two beyond six to the next scan, every one becomes a cross-member transfer with the HOST as the bench's source runner, and all eight end up as the client's real employees; ends dissolved, NO save",
  "preconditions": "Fixture save1 (the host melaus has a bench of unassigned staff that reaches the member; Client1 owns the shop 60 ba:street_fifthavenue). Build HQ-UNION-1 deployed (the 6-legs-per-scan budget in MergerEmployeeSync; the bench source runner in MPServer from fold HO-1c). The union card itself is UI (review-verified; the rig draws no screen). FIXTURE HYGIENE: eight host employees move to the client's shop in memory; NOTHING is saved.",
  "instances": 2,
  "vars": { "clientPid": "Client1", "hostPid": "melaus", "session": "save1", "shop": "60 ba:street_fifthavenue" },
  "oracles_absent": [
    "NullReferenceException",
    "HarmonyException",
    "Patching exception",
    "\\[Merger\\] accept .* REFUSED",
    "upload SKIPPED",
    "rate cap",
    "\\[Transfer\\] .*refused"
  ],
  "oracles_report": [
    "\\[Transfer\\] ",
    "\\[SharedShop\\] bench",
    "wait for the next scan"
  ],
  "steps": [
    { "seq": 1, "role": "h", "cmd": "host", "expect_result": "^OK" },
    { "seq": 2, "role": "c", "cmd": "join", "expect_result": "^OK" },
    { "seq": 3, "role": "h", "cmd": "hostload ${session}", "expect_result": "^OK" },
    { "seq": 4, "role": "both", "cmd": "mark TEST HQU1 START", "expect_result": "OK MARK" },
    { "seq": 5, "role": "h", "cmd": "merge propose ${clientPid}", "expect_result": "OK merge propose sent" },
    { "seq": 6, "role": "h", "sleep_s": 1 },
    { "seq": 7, "role": "h", "cmd": "offers", "expect_result": "pending=\\[${clientPid}<-${hostPid}\\]" },
    { "seq": 8, "role": "c", "cmd": "merge accept", "expect_result": "OK merge accept sent" },
    { "seq": 9, "role": "h", "sleep_s": 3 },
    { "seq": 10, "role": "h", "cmd": "mergestatus", "expect_result": "member=True.*flipped=[1-9]" },
    { "seq": 11, "role": "h", "sleep_s": 30, "note": "the client's world settles; the host's bench lands on the client" },
    { "seq": 12, "role": "c", "cmd": "status", "expect_result": "settled=True" },
    { "seq": 13, "role": "c", "cmd": "bench", "expect_result": "^OK bench ([89]|[1-9]\\d) ", "capture": {
        "b1": "^OK bench \\d+ ([^ :]+):",
        "b2": "^OK bench \\d+ [^ ]+ ([^ :]+):",
        "b3": "^OK bench \\d+ [^ ]+ [^ ]+ ([^ :]+):",
        "b4": "^OK bench \\d+ [^ ]+ [^ ]+ [^ ]+ ([^ :]+):",
        "b5": "^OK bench \\d+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ ([^ :]+):",
        "b6": "^OK bench \\d+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ ([^ :]+):",
        "b7": "^OK bench \\d+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ ([^ :]+):",
        "b8": "^OK bench \\d+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ [^ ]+ ([^ :]+):" },
      "note": "eight of the host's bench copies (at least eight must be there)" },
    { "seq": 14, "role": "c", "cmd": "assign ${b1} ${shop}", "expect_result": "^OK assigned" },
    { "seq": 15, "role": "c", "cmd": "assign ${b2} ${shop}", "expect_result": "^OK assigned" },
    { "seq": 16, "role": "c", "cmd": "assign ${b3} ${shop}", "expect_result": "^OK assigned" },
    { "seq": 17, "role": "c", "cmd": "assign ${b4} ${shop}", "expect_result": "^OK assigned" },
    { "seq": 18, "role": "c", "cmd": "assign ${b5} ${shop}", "expect_result": "^OK assigned" },
    { "seq": 19, "role": "c", "cmd": "assign ${b6} ${shop}", "expect_result": "^OK assigned" },
    { "seq": 20, "role": "c", "cmd": "assign ${b7} ${shop}", "expect_result": "^OK assigned" },
    { "seq": 21, "role": "c", "cmd": "assign ${b8} ${shop}", "expect_result": "^OK assigned", "note": "the rig sends one lever per second, so the 2 s scan never sees more than six moves at once and the U3 budget line cannot be provoked here (run 1, 2026-09-12) - the budget stays review-verified; what this scenario proves is the bench placement end to end" },
    { "seq": 22, "role": "c", "sleep_s": 25, "note": "both scans send; the host releases each record off its bench and holds it; the client adopts; the host's bench republishes without them" },
    { "seq": 23, "role": "h", "cmd": "mark TEST HQU1 CHECK", "expect_result": "OK MARK", "note": "the host's `stands on 'melaus' bench - they release` lines (HO-1c L2b, the host as the bench's source runner) land during the wait above and are collected by the oracle report, not asserted here (rig rule: expect_log searches from the step's own mark)" },
    { "seq": 24, "role": "c", "cmd": "employees ${shop}", "expect_result": "${b1}\\|[^|]*\\|assigned=${shop}\\|injected=False", "note": "the first placed worker is now the CLIENT's real employee at the shop (the ADDRESS-scoped list is uncapped; the bare list caps at 30 rows - run 3, 2026-09-12)" },
    { "seq": 25, "role": "c", "cmd": "employees ${shop}", "expect_result": "${b8}\\|[^|]*\\|assigned=${shop}\\|injected=False", "note": "the LAST one arrived too" },
    { "seq": 26, "role": "c", "cmd": "bench", "expect_result": "^OK bench \\d+", "refute_result": "${b8}:", "note": "the host's republished bench no longer lists the moved worker" },
    { "seq": 27, "role": "c", "cmd": "merge leave", "expect_result": "OK merge leave sent", "note": "FIXTURE HYGIENE: end dissolved; NO save" },
    { "seq": 28, "role": "h", "sleep_s": 5 },
    { "seq": 29, "role": "h", "cmd": "mergestatus", "expect_result": "member=False.*flipped=0" },
    { "seq": 30, "role": "both", "cmd": "mark TEST HQU1 END", "expect_result": "OK MARK" }
  ]
}
